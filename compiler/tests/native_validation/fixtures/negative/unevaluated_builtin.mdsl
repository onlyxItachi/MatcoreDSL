#include <matcore/mdsl.h>

namespace md = matcore::mdsl;

void unevaluated_builtin(md::matrix_view &C, const md::matrix_view &A,
                         const md::matrix_view &B) {
  (void)__builtin_constant_p(md::gemm(md::out(C), A, B, md::policy{}));
}
