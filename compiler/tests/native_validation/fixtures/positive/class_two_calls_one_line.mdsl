#include <matcore/mdsl.h>

namespace md = matcore::mdsl;

class PairOfOperations {
public:
  void execute(md::matrix_view &C, const md::matrix_view &A,
               const md::matrix_view &B) {
    md::gemm(md::out(C), A, B); md::gemm(md::out(C), A, B);
  }
};
