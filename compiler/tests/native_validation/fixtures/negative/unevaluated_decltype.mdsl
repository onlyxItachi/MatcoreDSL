#include <matcore/mdsl.h>

namespace md = matcore::mdsl;

void unevaluated_decltype(md::matrix_view &C, const md::matrix_view &A,
                          const md::matrix_view &B) {
  using result = decltype(md::gemm(md::out(C), A, B, md::policy{}));
  (void)sizeof(result *);
}
