#include <matcore/mdsl.h>

namespace md = matcore::mdsl;

void unevaluated_requires(md::matrix_view &C, const md::matrix_view &A,
                          const md::matrix_view &B) {
  (void)requires { md::gemm(md::out(C), A, B, md::policy{}); };
}
