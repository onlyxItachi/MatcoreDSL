#include "transitive_include.h"

namespace md = matcore::mdsl;

void no_direct_include(md::matrix_view &C, const md::matrix_view &A,
                       const md::matrix_view &B) {
  md::gemm(md::out(C), A, B);
}
