#include <matcore/mdsl.h>

namespace md = matcore::mdsl;

#define MATCORE_TEST_GEMM(C, A, B) md::gemm(md::out(C), A, B)

void rejected_macro(md::matrix_view &C, const md::matrix_view &A,
                    const md::matrix_view &B) {
  MATCORE_TEST_GEMM(C, A, B);
}
