#include "copied/matcore/mdsl.h"

namespace md = matcore::mdsl;

void copied_header(md::matrix_view &C, const md::matrix_view &A,
                   const md::matrix_view &B) {
  md::gemm(md::out(C), A, B);
}
